version: '3.8'

services:
  web:
    build: .
    container_name: zhivaya_kniga_web
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "9090:8000"
    environment:
      - DEBUG=1
      - DJANGO_SETTINGS_MODULE=zhivaya_kniga.settings